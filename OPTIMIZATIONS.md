# opencode-autorecord 优化说明

## 1. 子会话 idle 时缺少对父会话的 debounce（性能/重复写入问题）

**问题描述：**
当子会话（subagent task）触发 idle 事件时，父会话的保存逻辑也会被频繁触发，导致同一父会话被重复写入多次，造成不必要的 I/O 开销。

**优化方案：**
为父会话添加防抖机制（debounce）。当子会话 idle 时，不再立即触发父会话保存，而是等待一段静默时间，确保所有子会话的写入操作都完成后，再统一保存一次父会话。

**效果：**
- 减少重复写入次数
- 降低磁盘 I/O 压力
- 避免生成大量中间状态的文件

---

## 2. 子会话读取没有错误隔离（健壮性问题）

**问题描述：**
读取子会话数据时，如果某个子会话文件损坏或读取失败，会导致整个父会话的保存流程中断，影响数据完整性。

**优化方案：**
对每个子会话的读取操作添加 try-catch 错误隔离。当某个子会话读取失败时，记录错误日志但继续处理其他子会话，不会中断整体保存流程。

**效果：**
- 单个子会话失败不影响整体保存
- 提高系统健壮性和容错能力
- 避免因为一个损坏文件导致全部数据丢失

---

## 3. `session.deleted` 时存在数据丢失风险（时序问题）

**问题描述：**
在会话被删除（`session.deleted`）时，保存操作可能尚未完成，导致最后一部分对话内容丢失。这是一个典型的竞态条件（race condition）问题。

**优化方案：**
优化 `session.deleted` 事件的处理逻辑，确保在会话真正删除之前，所有待保存的数据都已经写入磁盘。可能需要添加同步等待或队列机制。

**效果：**
- 避免最后几条消息丢失
- 保证数据完整性
- 解决时序竞态问题

---

## 4. `convertMessages` 是伪异步函数（代码质量问题）

**问题描述：**
`convertMessages` 函数虽然声明为 async，但内部并没有真正的异步操作（如 I/O、网络请求等），属于不必要的异步包装。这会导致：
- 代码可读性降低
- 调用方需要不必要的 await
- 微任务队列增加额外开销

**优化方案：**
将 `convertMessages` 改为同步函数，移除不必要的 async/await 关键字。如果调用方需要异步，可以在调用处包装。

**效果：**
- 代码更清晰，符合实际逻辑
- 减少不必要的 Promise 开销
- 提高执行效率

---

## 5. 图片处理未并行化（性能问题）

**问题描述：**
处理消息中的图片时，采用串行方式逐个处理，当消息中包含多张图片时，处理时间较长。

**优化方案：**
使用 `Promise.all()` 或 `Promise.allSettled()` 并行处理多张图片，充分利用异步 I/O 的并发能力。

**效果：**
- 多张图片并行下载/保存
- 显著减少图片处理时间
- 提升整体保存速度

---

## 6. 没有消息缓存，每次 idle 都请求全量历史（性能问题）

**问题描述：**
每次会话 idle 触发保存时，都重新获取完整的会话历史消息，即使只有少量新消息。这导致：
- 重复处理大量已保存过的消息
- 增加内存和 CPU 开销
- 保存延迟增加

**优化方案：**
引入消息缓存机制，记录上次保存时的消息状态。下次保存时，只处理新增的消息，与已有内容合并。

**效果：**
- 增量保存，减少重复处理
- 降低内存占用
- 加快保存速度
- 减少 I/O 操作

---

## 7. 不保存到项目目录，集中保存在 ~/opencode-autorecord（架构优化）

**问题描述：**
原方案将会话记录保存在各自的项目目录中，导致：
- 会话记录分散在各个项目
- 难以统一管理和查找
- 可能污染项目代码库
- 切换项目时历史记录不连贯

**优化方案：**
将所有会话记录集中保存在用户主目录下的 `~/opencode-autorecord` 文件夹中，按项目名称分子目录存储。

**效果：**
- 会话记录统一管理
- 不污染项目代码
- 跨项目查看历史更方便
- 便于备份和归档
- 目录结构清晰：`~/opencode-autorecord/<project-name>/<timestamp>-<topic>.md`

---

## 8. 删除项目后残留 HTML 页面未清理（资源/健壮性问题）

**问题描述：**
手动删除项目文件夹后，`validateAndRepairIndexes` 的孤儿项目清理会从索引移除该项目，但 `projects/<项目名>.html` 页面文件不会随之删除，长期累积占用磁盘空间。

**优化方案：**
每次视图再生成时执行 `cleanupStaleProjectPages()`：对比 `projects/` 目录下的 `.html` 文件名（`decodeURIComponent` 解码）与当前项目列表，删除已不存在项目对应的页面。清理独立于项目是否存在（全部项目被删时也能执行），失败静默忽略，删除数量写入 `.autorecord-views.log`。

**效果：**
- 删除项目后对应页面同步消失
- 避免残留 HTML 文件长期堆积
- 清理失败不影响视图生成主流程

---

## 总结

| 编号 | 优化点 | 类型 | 影响 |
|------|--------|------|------|
| 1 | 父会话 debounce | 性能 | 减少重复写入 |
| 2 | 子会话错误隔离 | 健壮性 | 单点失败不影响整体 |
| 3 | session.deleted 时序 | 可靠性 | 避免数据丢失 |
| 4 | convertMessages 同步化 | 代码质量 | 消除伪异步 |
| 5 | 图片并行处理 | 性能 | 加速图片保存 |
| 6 | 消息缓存机制 | 性能 | 增量保存 |
| 7 | 集中存储目录 | 架构 | 统一管理和查找 |
| 8 | 残留 HTML 清理 | 资源/健壮性 | 删除项目后同步移除页面 |

这些优化从**性能**、**健壮性**、**可靠性**和**代码质量**四个维度提升了插件的整体表现。
